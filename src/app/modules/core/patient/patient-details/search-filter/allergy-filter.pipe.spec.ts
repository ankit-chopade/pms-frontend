import { AllergyFilterPipe } from './allergy-filter.pipe';

describe('AllergyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AllergyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
