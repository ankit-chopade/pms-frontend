// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  baseUrl:'http://localhost:8989/',
   allergyUrl:'http://localhost:7001/',
   diagnosisUrl:'http://localhost:7002/',
   medicationUrl:'http://localhost:7003/',
   procedureUrl:'http://localhost:7004/',
   managementUrl:'http://localhost:7005/',
   scheduleUrl:'http://localhost:7006/',
   visitUrl:'http://localhost:7007/',

  // allergyUrl:'http://localhost:8989/',
  // diagnosisUrl:'http://localhost:8989/',
  // medicationUrl:'http://localhost:8989/',
  // procedureUrl:'http://localhost:8989/',
  // managementUrl:'http://localhost:8989/',
  // scheduleUrl:'http://localhost:8989/',
  // visitUrl:'http://localhost:8989/',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
